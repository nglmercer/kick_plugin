{
  "name": "kick_plugin",
  "description": "kick plugin emit events in context",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bun_plugins": "^1.2.7",
    "kick-wss": "^1.0.5",
    "trigger_system": "^1.3.1"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --entry-naming \"[dir]/kick_plugin.[ext]\"",
    "start": "bun ./src/index.ts"
  }
}
